# STA258_Quiz_Questions
Quiz Questions for STA258

## Quiz 9: Things to do (Rithika)

- Qustion to plot $y$ vs. $x$, examine whether there is a linear trend
- Question to numerically estimate $\hat{\beta}_0$ [AND]
- Question to numerically estimate $\hat{\beta}_1$
- Question to interpret the intercept of a regression model (MCQ)
  - Both +ve and -ve intercepts
  - Use model unrelated to data set used so students (but state this)
- Question to interpret the slope of a regression model (MCQ)
  - Both +ve and -ve intercepts
  - Use model unrelated to data set used so students (but state this)
- Change residual question to calculate the residual for existing data point
- Question to interpret the meaning of residual (MCQ)


## Quiz 10: Things to do (Giodanno)

- Qustion to plot $y$ vs. $x$, examine whether there is a linear trend
- Change the data set to something with more realistic linear trend
- After inference on slope, have a interpolation prediction
  - Follow-up: Whether interpolation prediction is reliable (MCQ)
- After inference on slope, have an extrapolation prediction
  - Follow-up: Whether extrapolation prediction is reliable (MCQ)
- Inclide question on residual plot (Multi-select)
  - Select all features present in the plot which suggest good/bad model
