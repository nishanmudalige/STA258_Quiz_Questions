---
title: "quiz4a"
title-block-style: none
format: live-html
engine: knitr
sidebar:
  style: docked
resources:
  - Academic_Performance_B.csv
---

{{< include /_extensions/r-wasm/live/_knitr.qmd >}}
{{< include /_extensions/r-wasm/live/_gradethis.qmd >}}

<script>
document.addEventListener("DOMContentLoaded", () => {
  const params = new URLSearchParams(window.location.search);
  const showQ = params.get("q");

  // Show only the requested section
  if (showQ) {
    document.querySelectorAll("section[id]").forEach(section => {
      if (section.id !== showQ) section.style.display = "none";
    });
  }

  // Hide page title
  document.querySelectorAll(".quarto-title-block")
    .forEach(el => el.style.display = "none");

  // Hide ALL section titles (## ...)
  document.querySelectorAll("section[id] > h2")
    .forEach(h => h.style.display = "none");
});
</script>

## Q1 — Z Critical Value {#q1}
What is the 90% two-sided $Z^*$ critical value?
```{webr}

```

```{webr}
#| echo: false
#| output: false
qnorm(0.95)
#1.6449
```
## Q2 — Z Confidence Interval (Lower Bound) {#q2}
Using `Exam_Score`, assume σ=10.

Construct a 90% Z confidence interval for the true mean exam score.

What is the lower bound of this interval?
```{webr}
df <- read.csv("Academic_Performance_A.csv")
x <- df$Exam_Score
xbar <- ___
n <- ___
sigma <- ___
z <- ___

```

```{webr}
#| echo: false
#| output: false
df <- read.csv("Academic_Performance_A.csv")
x <- df$Exam_Score
xbar <- mean(x)
n <- length(x)
sigma <- 10
z <- qnorm(0.95)
xbar - z*sigma/sqrt(n)
# 79.0485
```
## Q3 — Z Confidence Interval (Upper Bound) {#q3}
Using `Exam_Score`, assume σ=10.

Construct a 90% Z confidence interval for the true mean exam score.

What is the upper bound of this interval?
```{webr}
df <- read.csv("Academic_Performance_A.csv")
x <- df$Exam_Score
xbar <- ___
n <- ___
sigma <- ___
z <- ___

```

```{webr}
#| echo: false
#| output: false
df <- read.csv("Academic_Performance_A.csv")
x <- df$Exam_Score
xbar <- mean(x)
n <- length(x)
sigma <- 10
z <- qnorm(0.95)
xbar + z*sigma/sqrt(n)
# 82.7265
```
## Q4 — t Critical Value {#q4}
Using `Final_Exam_Score`:

What is the 90% two-sided t* critical value?
```{webr}
df <- read.csv("Academic_Performance_A.csv")
n <- length(df$Final_Exam_Score)
```

```{webr}
#| echo: false
#| output: false
df <- read.csv("Academic_Performance_A.csv")
n <- length(df$Final_Exam_Score)
qt(0.95, df = n - 1)
#1.6644
```
## Q5 — t Confidence Interval (Lower Bound) {#q5}
Construct a 90% t confidence interval for the true mean `Final_Exam_Score`.

What is the lower bound of this interval?
```{webr}
df <- read.csv("Academic_Performance_A.csv")
x <- df$Final_Exam_Score
xbar <- ___
s <- ___
n <- ___
tstar <- ___
```

```{webr}
#| echo: false
#| output: false
df <- read.csv("Academic_Performance_A.csv")
x <- df$Final_Exam_Score
xbar <- mean(x)
s <- sd(x)
n <- length(x)
tstar <- qt(0.95, df = n - 1)
xbar - tstar*s/sqrt(n)
#78.9316
```

## Q6 — t Confidence Interval (Upper Bound) {#q6}
Construct a 90% t confidence interval for the true mean `Final_Exam_Score`.

What is the upper bound of this interval?
```{webr}
df <- read.csv("Academic_Performance_A.csv")
x <- df$Final_Exam_Score
xbar <- ___
s <- ___
n <- ___
tstar <- ___
```

```{webr}
#| echo: false
#| output: false
df <- read.csv("Academic_Performance_A.csv")
x <- df$Final_Exam_Score
xbar <- mean(x)
s <- sd(x)
n <- length(x)
tstar <- qt(0.95, df = n - 1)
xbar + tstar*s/sqrt(n)
#83.9934
```

## Q7 — Proportion CI (Lower Bound){#q7}
Construct a 90% Z confidence interval for the true admission proportion.

What is the lower bound of this interval?
```{webr}
df <- read.csv("Academic_Performance_A.csv")
x <- df$Admitted
n <- ___
phat <- ___
z <- ___

```

```{webr}
#| echo: false
#| output: false
df <- read.csv("Academic_Performance_A.csv")
x <- df$Admitted
n <- length(x)
phat <- mean(x)
z <- qnorm(0.95)
phat - z*sqrt(phat*(1-phat)/n)
#0.6704
```

## Q8 — Proportion CI (Upper Bound){#q8}
Construct a 90% Z confidence interval for the true admission proportion.

What is the upper bound of this interval?
```{webr}
df <- read.csv("Academic_Performance_A.csv")
x <- df$Admitted
n <- ___
phat <- ___
z <- ___

```

```{webr}
#| echo: false
#| output: false
df <- read.csv("Academic_Performance_A.csv")
x <- df$Admitted
n <- length(x)
phat <- mean(x)
z <- qnorm(0.95)
phat + z*sqrt(phat*(1-phat)/n)
#0.8296
```
